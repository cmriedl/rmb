<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ .Title }}</title>
        <meta charset="utf-8">
        <link rel="stylesheet" type="text/css" href="all.css"/>
    </head>
    <body>
</pre>
{{ if .Index }}
    <div>{{ .Body | tostring }}</div>
    {{ if .IndexElems }}
        {{ range .IndexElems }}
            <div>{{ .Date }}<a href="{{ .Link }}">{{ .Title }}</a></div>
        {{ end }}
    {{ end }}
{{ else }}
    <h1>{{ .Title }}</h1>
    <body>{{ .Body | tostring }}</body>
{{ end }}
</html>
